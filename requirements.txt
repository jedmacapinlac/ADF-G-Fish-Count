# Data acquisition — runs on the laptop (pandas is fine in short-lived processes)
pandas>=2.0
openpyxl>=3.1
requests>=2.31
beautifulsoup4>=4.12

# Database
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0

# API — runs on the EC2. Never import pandas here; aggregate in SQL instead.
fastapi>=0.110
uvicorn[standard]>=0.29
orjson>=3.9
